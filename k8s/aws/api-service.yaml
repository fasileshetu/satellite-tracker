apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: satellite-tracker
spec:
  type: NodePort # exposes it outside the cluster on the node's IP, good enough for local dev
  selector:
    app: api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
