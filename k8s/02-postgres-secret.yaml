# For local dev only. In a real cluster this would come from a secrets
# manager, not be checked into git with plaintext values.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: satellite-tracker
type: Opaque
stringData:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  POSTGRES_DB: satellite_tracker
